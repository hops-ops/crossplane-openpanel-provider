# Repository Guidelines

Avoid Upbound-hosted configuration packages when alternatives exist; prefer crossplane-contrib packages. Target Crossplane 2+: do not set deletionPolicy on managed resources; use managementPolicies and defaults.
